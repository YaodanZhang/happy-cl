1Password client folder
---

Please keep 1Password client (`op`) in this folder, keep the name as `op`.

DO NOT add `op` to the `$PATH`

If the file name is changed, please make sure the `.profile` file get changed
accordingly.
